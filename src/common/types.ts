export type Curso = {
  id: number;
  nombre: string;
  picture: string;
  description: string;
};

export type TokenUser = {
  id: string;
};

export type Input = {
  name: string;
  type: string;
  label: string;
  placeholder: string;
};
