export type Curso = {
  id: number;
  title: string;
  picture: string;
  description: number;
};

export type TokenUser = {
  id: string;
};

export type Input = {
  name: string;
  type: string;
  label: string;
  placeholder: string;
};
